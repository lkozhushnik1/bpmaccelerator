import { IntakePage } from '@/components/process/IntakePage'

export const metadata = {
  title: 'New Process — BPM Accelerator',
  description: 'Describe your business process and generate a normalized model, graph, agent plan, and deployment artifacts.',
}

export default function Page() {
  return <IntakePage />
}
